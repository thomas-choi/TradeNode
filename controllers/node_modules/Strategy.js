// JScript source code
var SM = require('./SubMgr');
var APL = require('./Log');
var PF = require('./TickData');

function start(symbol) {
    APL.log('Strategy started');
    SM.on(symbol, CB);
}

function CB(Price) {
//        APL.log('Strategy on:' + Price.toString());
}

module.exports.start = start;