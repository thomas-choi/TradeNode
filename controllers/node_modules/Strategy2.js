// JScript source code
var SM = require('./SubMgr');
var APL = require('./Log');
var PF = require('./Price');

function start(symbol) {
    APL.log('Strategy2 started');
    SM.on(symbol, CB);
}

function CB(Price) {
//        APL.log('Strategy2 on:' + Price.toString());
}

module.exports.start = start;
